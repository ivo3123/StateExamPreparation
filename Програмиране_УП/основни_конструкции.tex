\documentclass{scrartcl}
\usepackage{graphicx}  % required for inserting images

% for cyrillic and Bulgarian language
\usepackage[utf8]{inputenc}
\usepackage[T2A]{fontenc}
\usepackage[bulgarian]{babel}

% math packages
\usepackage{amsmath}
\usepackage{float}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{amsthm}
\usepackage{cancel}

\usepackage{ragged2e}  % adds FlushLeft

\usepackage{listings}
\usepackage{xcolor}

\definecolor{codegreen}{rgb}{0,0.6,0}
\definecolor{codegray}{rgb}{0.5,0.5,0.5}
\definecolor{codepurple}{rgb}{0.58,0,0.82}
\definecolor{backcolour}{rgb}{0.95,0.95,0.92}

\lstdefinestyle{mystyle}{
    % caption=Example C++,
    label={lst:listing-cpp},
    language=C++,
    backgroundcolor=\color{backcolour},   
    commentstyle=\color{codegreen},
    keywordstyle=\color{magenta},
    numberstyle=\tiny\color{codegray},
    stringstyle=\color{codepurple},
    basicstyle=\ttfamily\footnotesize,
    breakatwhitespace=false,         
    breaklines=true,                 
    keepspaces=true,                 
    numbers=left,       
    numbersep=5pt,                  
    showspaces=false,                
    showstringspaces=false,
    showtabs=false,                  
    tabsize=2,
}
\lstset{style=mystyle}

\usepackage[colorlinks=true, linkcolor=blue, urlcolor=cyan]{hyperref}

\newtheorem{theorem}{Теорема}
\newtheorem{definition}{Дефиниция}

\title{Увод в програмирането - основни конструкции}
\author{Ивайло Андреев + gemini 3 Flash, chat GPT 5.4}
\date{25 май 2026}

\begin{document}
\maketitle

\tableofcontents

\newpage

\begin{FlushLeft}

\section{Принципи на структурното програмиране}
Процедурната парадигма служи като основа за разбирането на съвременните подходи и е неизменна част от изучаването на алгоритми и структури от данни. Програмирането може да бъде разделено на две основни парадигми:
\begin{itemize}
    \item \textbf{Императивно програмиране:} Описва \textit{как} се изпълнява дадена задача, чрез поредица от инструкции, които променят състоянието на програмата (например чрез променливи и команди за контрол на потока). То включва процедурното и обектно-ориентираното програмиране.
    \item \textbf{Декларативно програмиране:} Описва \textit{какво} трябва да се постигне, без изрично да се указват стъпките за изпълнение (например функционално програмиране и логическо програмиране).
\end{itemize}

Преди структурното програмиране програмите често са били изграждани като линейна последователност от инструкции с множество безусловни преходи (\texttt{goto}) за реализиране на условна логика и повторение на операции. Това е водело до трудно четим и труден за поддръжка код (,,spaghetti code``).

Структурното програмиране въвежда организиране на програмата чрез ясно дефинирани контролни конструкции и блокове:
\begin{itemize}
    \item последователност;
    \item условни конструкции (\texttt{if}, \texttt{switch});
    \item цикли (\texttt{for}, \texttt{while}, \texttt{do-while});
    \item функции и процедури.
\end{itemize}
Основен принцип е избягването на хаотични преходи чрез \texttt{goto} и изграждането на програми с ясен и предвидим контролен поток. Това подобрява четимостта, поддръжката и надеждността на кода. Блоковете могат да имат собствен scope и локални променливи, което улеснява организирането на програмата.

\section{Управление на изчислителния процес}
Управлението на изчислителния процес се осъществява чрез инструкции, които определят реда на изпълнение на действията. Програмата се състои от изрази (които пресмятат и връщат стойност) и оператори (които извършват действия). Блок от код се огражда с къдрави скоби \texttt{\{\}}.

\subsection{Условни оператори}
Условните оператори позволяват разклоняване на логиката. Използват се \texttt{if}, \texttt{if-else} и \texttt{switch} за многозначно разклоняване.
\begin{lstlisting}
// Example of conditional statements and basic syntax
int x = 10;
if (x > 5) {
    // Executed if the condition is true
    x--;
} else {
    // Executed if the condition is false
    x++;
}

// Inline ternary conditional choice
int y = (x == 9) ? 1 : 0; 
// Equivalent using if-else:
// int y;
// if (x == 9) { y = 1; } else { y = 0; }

// Switch selector statement - without break, execution falls through to the next cases
switch (y) {
    case 0:
        // Handle case 0 block
        break; // Stop fall-through
    case 1:
        // Handle case 1 block
        break;
    default:
        // Optional catch-all block
        break;
}
\end{lstlisting}

\subsection{Оператори за цикъл}
Циклите осигуряват многократно изпълнение на код. Те се делят на цикли с предусловие (\texttt{while}), цикли с постусловие (\texttt{do-while}) и цикли с брояч (\texttt{for}).
\begin{lstlisting}
// 1. Pre-condition loop (evaluates condition before running body)
int i = 0;
while (i < 5) {
    // Loop code statements
    i++;
}

// 2. Post-condition loop (guaranteed to execute at least once)
int j = 0;
do {
    // Executes body, then evaluates condition
    j++;
} while (j < 5);

// 3. Counter-controlled loop iteration (k is local to the for loop)
for (int k = 0; k < 5; k++) {
    // Initialization, condition check, and step execution
}

// break terminates the loop; continue skips the rest of the current iteration
for (int i = 0; i < 10; i++) {
    if (i == 3) continue; // Skip i == 3
    if (i == 7) break;    // Stop at i == 7
}
\end{lstlisting}

\section{Променливи}
Променливата е наименувана област от паметта, която съхранява стойност от определен тип. Всяка променлива има адрес, съответстващ на позицията ѝ в паметта.

\subsection{Локални и глобални променливи}
Основните разлики между променливите се свеждат до техния обхват (видимост) и време на живот:
\begin{itemize}
    \item \textbf{Локални променливи:} Дефинират се вътре във функция или блок \texttt{\{\}}. Те се създават при достигане на дефиницията им и се унищожават при излизане от блока (разполагат се в \textbf{Стека}). Видими са само в този блок.
    \item \textbf{Глобални променливи:} Дефинират се извън всички функции, в началото на файла. Те съществуват през цялото време на изпълнение на програмата (разположени в \textbf{Data/BSS сегмента}) и са достъпни от всяка функция в рамките на файла.
\end{itemize}

\subsection{Инициализация и оператор за присвояване}
\begin{itemize}
    \item \textbf{Оператор за присвояване (\texttt{=}):} Променя текущата стойност на вече съществуваща променлива, като оценява израза отдясно и го записва в паметта на променливата отляво.
    \item \textbf{Инициализация:} Задаване на начална стойност на променливата в момента на нейното деклариране. Важно е да се отбележи, че неинициализираните локални променливи съдържат произволни данни от паметта (т.нар. "боклук"), което води до грешки и недефинирано поведение, докато глобалните се инициализират автоматично с 0.
\end{itemize}
\begin{lstlisting}
int globalVar;       // Global variable, auto-initialized to 0

int main() {
    int a;           // Local variable, uninitialized (contains garbage)
    a = 5;           // Assignment: changes the current value
    int b = 10;      // Initialization: sets value at declaration
}
\end{lstlisting}

\section{Функции и процедури}
Функцията е самостоятелен блок от код, който решава подзадача и може да бъде извикан многократно. В C++ процедурите се представят като функции с тип на връщане \texttt{void}.
\begin{itemize}
    \item \textbf{Формални параметри:} Променливите, изброени в заглавието на функцията.
    \item \textbf{Фактически параметри (Аргументи):} Реалните стойности или променливи, които се подават в скобите при извикването.
\end{itemize}
\textbf{Предимства на функциите:}
\begin{itemize}
    \item Повторно използване на код;
    \item Подобрява четимостта и структурата;
    \item Улеснява поддръжката и тестването;
    \item Подпомага модулността и разделянето на задачите на логически части.
\end{itemize}

\subsection{Предаване на параметри}
Основните механизми за предаване на параметри:
\begin{itemize}
    \item \textbf{Предаване по стойност (Pass by Value):} Създава се локално копие на аргумента в стековата рамка на функцията. Промените вътре във функцията не влияят на оригиналната променлива.
    \item \textbf{Предаване по референция (Pass by Reference):} Подава се референция към оригиналната променлива. Промените вътре във функцията влияят директно на оригинала.
    \item \textbf{Предаване по указател (Pass by Pointer):} Подава се адресът на оригиналната променлива. Промените чрез дерефериране на указателя влияят на оригинала.
\end{itemize}
\begin{lstlisting}
// Demonstrating parameter passing models
void modifyValue(int num) {
    // Pass by value: changes only the local stack copy
    num = 100; 
}

void modifyByReference(int& num) {
    // Pass by reference: changes the original argument
    num = 100;
}

void modifyByPointer(int* num) {
    // Pass by pointer: dereference to change the original
    *num = 100;
}

int main() {
    int x = 5;

    modifyValue(x);        // x remains 5
    modifyByReference(x);  // x becomes 100
    modifyByPointer(&x);   // x becomes 100

    return 0;
}
\end{lstlisting}
Референцията е синтактична захар за указател. Основните разлики между тях:
\begin{itemize}
    \item Референцията е винаги валидна --- тя трябва да бъде инициализирана при декларацията; указател може да е \texttt{nullptr}.
    \item Референцията не може да бъде пренасочвана --- след инициализация тя винаги сочи към същия обект; указател може да се пренасочи към друг обект.
    \item Референцията не може да е нулева --- \texttt{nullptr} референция е недефинирано поведение.
\end{itemize}

\subsection{Анонимни функции (Ламбда функции)}
Функциите могат да бъдат и анонимни --- без име, или наричани още ламбда функции. Обща дефиниция:

%\begin{lstlisting}
[<прихващащ клас>] (<формални параметри>) { <тяло> }
%\end{lstlisting}

Прихващащият клас може да е \texttt{[\&]} или \texttt{[=]}:
\begin{itemize}
    \item \texttt{[\&]} --- променливите са прихванати по референция;
    \item \texttt{[=]} --- променливите са прихванати по стойност (по подразбиране).
\end{itemize}

Тялото се очаква да завършва с израз с return.

Пример: сортиране на масив с анонимна функция:
\begin{lstlisting}
int arr[] = {3, 1, 2};
std::sort(begin(arr), end(arr), [&](int a, int b) { return a < b; });
\end{lstlisting}

\subsection{Типове и проверка за съответствие на тип}
C++ е статично типизиран език. Компилаторът извършва строга проверка за съответствие на типовете (\textbf{Type Checking}) по време на компилация. Когато подадем аргумент на функция, компилаторът проверява дали типът му съвпада с дефинирания формален параметър. При несъвпадение се прави опит за автоматично (неявно) преобразуване на типа, а ако такова е невъзможно, компилацията се прекъсва с грешка за несъвместимост.

Основните примитивни типове в C++ включват:
\begin{itemize}
    \item \texttt{int} --- цяло число (обикновено 4 байта);
    \item \texttt{double}, \texttt{float} --- числа с плаваща запетая;
    \item \texttt{char} --- единичен символ;
    \item \texttt{bool} --- булева стойност (\texttt{true} / \texttt{false});
    \item \texttt{void} --- липса на тип (използва се за функции, които не връщат стойност).
\end{itemize}
Понеже C++ е статично типизиран, той поддържа \textbf{предефиниране на функции по параметри (function overloading)} --- няколко функции могат да имат едно и също име, но различни типове или брой параметри. Компилаторът избира подходящата версия въз основа на типовете на аргументите.
\begin{lstlisting}
int    add(int a,    int b)    { return a + b; }
double add(double a, double b) { return a + b; }
\end{lstlisting}

\end{FlushLeft}

\end{document}